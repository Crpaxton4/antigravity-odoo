---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: antigravity-odoo
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://github.com/Crpaxton4/antigravity-odoo.git
  ref:
    branch: main
  # For private repos, create a secret:
  # secretRef:
  #   name: git-credentials
